{
    "name": "qr-scanner-monorepo",
    "version": "1.0.0",
    "scripts": {
        "build": "cd client && npm install && npm run build"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "googleapis": "^126.0.0",
        "nodemailer": "^6.9.4",
        "xlsx": "^0.18.5"
    }
}